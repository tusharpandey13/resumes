# build.conf — Resume pipeline configuration
# Sourced by scripts/build-resume.sh

# Output PDF filename (without path — applied inside the output directory)
OUTPUT_FILENAME="Tushar-Pandey-resume.pdf"
