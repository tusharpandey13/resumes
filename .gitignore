# LaTeX aux files
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.lof
*.lot

# Compiled PDFs (tracked explicitly per variant)
# Uncomment the line below to exclude all PDFs
# *.pdf

# macOS
.DS_Store

# Input variants — gitignored (contains company slugs in dir names)
applications/
2026/

# Local application index — never published
APPLICATIONS.md

